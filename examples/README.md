AioExecutor Examples
====================

This directory contains example applications that demonstrate the `AioExecutor`
class and the `run_with_asyncio` decorator.

quotes.py
---------

A Flask application that demonstrates how to write an async view function that
Flask can use with the `run_with_asyncio` decorator.
